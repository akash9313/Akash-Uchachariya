#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cout<<"Enter the size of arr : ";
    cin>>n;

    int arr[n];
    cout<<"Enter the element of arr : ";
    for (int i = 0; i < n; i++)
    {
        cin>>arr[i];
    }

    Qui
    

    return 0;
}